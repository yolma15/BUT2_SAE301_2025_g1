<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Paiement</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/css/prix.css">
</head>
<body>
  <div class="payment-container">

    <!-- FORMULAIRE -->
    <form class="payment-form" id="payment-form">
      <h1>Paiement sécurisé</h1>

      <div class="section">
        <h2>Informations de carte</h2>

        <label for="card-name">Nom sur la carte</label>
        <input type="text" id="card-name" placeholder="Quentin Dupont" required>

        <label for="card-number">Numéro de carte</label>
        <input type="text" id="card-number" maxlength="19" placeholder="1234 5678 9012 3456" required>

        <div class="row">
          <div class="field">
            <label for="card-expiry">Expiration</label>
            <input type="text" id="card-expiry" placeholder="MM / AA" required>
          </div>
          <div class="field">
            <label for="card-cvv">CVV</label>
            <input type="password" id="card-cvv" maxlength="4" placeholder="123" required>
          </div>
        </div>
      </div>

      <div class="section">
        <h2>Adresse de facturation</h2>

        <label for="full-name">Nom complet</label>
        <input type="text" id="full-name" placeholder="Quentin Dupont" required>

        <label for="address">Adresse</label>
        <input type="text" id="address" placeholder="12 rue des Designers" required>

        <div class="row">
          <div class="field">
            <label for="city">Ville</label>
            <input type="text" id="city" placeholder="Lannion" required>
          </div>
          <div class="field">
            <label for="zip">Code postal</label>
            <input type="text" id="zip" placeholder="22300" required>
          </div>
        </div>
      </div>

      <div class="summary">
        <span>Total</span>
        <strong>49,90 €</strong>
      </div>

      <button type="submit" class="btn-primary">Payer maintenant</button>
      <p class="helper-text">Tes données sont chiffrées et transmises de façon sécurisée.</p>
    </form>

    <!-- MESSAGE DE CONFIRMATION -->
    <div class="success-message hidden" id="success-message">
      <div class="success-icon">✓</div>
      <h1>Paiement validé</h1>
      <p>Merci pour votre achat.</p>
      <p>Un e‑mail de confirmation vient de vous être envoyé.</p>
    </div>
  </div>

  <script>
    const form = document.getElementById('payment-form');
    const success = document.getElementById('success-message');

    form.addEventListener('submit', function (event) {
      event.preventDefault();

      // Ici tu peux ajouter l'appel à ton backend / API si besoin

      form.classList.add('hidden');
      success.classList.remove('hidden');
    });
  </script>
</body>
</html>
